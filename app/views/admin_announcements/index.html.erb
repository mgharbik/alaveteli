<div class="row">
  <div class="span12">
    <div class="page-header">
      <h1><%= _('Announcements') %></h1>
    </div>
  </div>
</div>

<%= render :partial => 'show',
           :locals => { :announcements => @announcements } %>
